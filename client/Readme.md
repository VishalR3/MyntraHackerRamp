This React Client is built using Vite Package Bundler

- To Start a dev Server use `npm run dev`
- For Building the distributable version, use `npm run build`
- To Preview the build version, use `npm run serve`
